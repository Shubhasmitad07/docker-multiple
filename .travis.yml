sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t shubhasmitad07/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true shubhasmitad07/react-test npm test

after_success:
  - docker build -t shubhasmitad07/multi-client ./client
  - docker build -t shubhasmitad07/multi-nginx ./nginx
  - docker build -t shubhasmitad07/multi-server ./server
  - docker build -t shubhasmitad07/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shubhasmitad07/multi-client
  - docker push shubhasmitad07/multi-nginx
  - docker push shubhasmitad07/multi-server
  - docker push shubhasmitad07/multi-worker
